{% extends 'YabeMainBundle:Home:index.html.twig' %}

{% block body -%}
    <h1>SocialNetwork list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Facebook</th>
                <th>Twitter</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('socialnetwork_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.facebook }}</td>
                <td>{{ entity.twitter }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('socialnetwork_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('socialnetwork_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('socialnetwork_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
